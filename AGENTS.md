# 修改要求

- 修改前先检查相关文件和工作区状态，保留用户已有内容及无关改动。
- 只实施用户明确要求的功能，不擅自扩大任务范围。
- 未经用户明确要求，不实现目标跟踪、卡尔曼滤波、运动状态分析、多源融合、模型训练或前端界面。
- Python 路径处理统一使用 `pathlib`。
- 新增或修改的 Python 代码应包含类型标注，并仅添加有助于理解关键逻辑的注释。
- 面向用户的运行状态和错误提示优先使用清晰的中文。
- 未经用户确认，不下载测试视频、模型权重或其他大型文件，不启动模型训练。
- 未经用户确认，不执行全局安装命令，不删除或修改其他 Conda 环境。
- 安装项目依赖时仅使用 `visual_analysis` Conda 环境，并优先通过 `python -m pip` 调用该环境的 pip。
- 修改完成后执行与改动相匹配的非破坏性检查；至少检查 Python 语法，并在可行时验证缺失输入等错误路径。
- 不把测试视频、模型权重、生成的视频、密钥、认证信息或本机环境目录提交到版本控制。
